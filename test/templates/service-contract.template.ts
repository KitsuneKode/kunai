// Copy this file when adding a service-level contract test under `test/integration/`.

import { describe, expect, test } from "bun:test";

describe("service contract template", () => {
  test.skip("asserts on stable behavior instead of incidental implementation details", () => {});
  test.skip("keeps external boundaries stubbed and deterministic", () => {});

  test("replace this template with a real service contract test", () => {
    expect(true).toBe(true);
  });
});
