# Legacy Reference Archive

This directory stores reference-only legacy runtime code moved out of the active CLI path.

- Do not import from `archive/legacy/**` in production runtime code.
- Keep this archive only for parity research and migration context.
- Prefer deleting archived code once it no longer informs provider/runtime decisions.
