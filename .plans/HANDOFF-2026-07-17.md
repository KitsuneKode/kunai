# Session Handoff — 2026-07-17

Start the next session with:

> Read `.plans/HANDOFF-2026-07-17.md` and continue from it. Do not start another broad audit. Complete the startup fix first, then execute the Notifications Inbox v2 plan with subagent-driven development.

## Authoritative scope

There are only two active workstreams:

1. Finish and verify the startup-latency fix already in the working tree.
2. Execute `docs/superpowers/plans/2026-07-16-notifications-inbox-v2.md` task-by-task.

Do not reopen the broad queue/notifications/playlists audit. Up Next is already implemented. Durable Playlists remains a later independent slice.

## Completed

### Notifications design

- Approved design committed as `bce2aa5b`:
  - `docs/superpowers/specs/2026-07-16-notifications-inbox-v2-design.md`
- Detailed six-task implementation plan written at:
  - `docs/superpowers/plans/2026-07-16-notifications-inbox-v2.md`
- User selected **subagent-driven execution**.
- The implementation itself has not started.

### Startup investigation

Root-cause work is complete. Do not repeat it.

Confirmed:

- No hidden React/Ink timeout caused the delay.
- Durable diagnostics, SQLite pruning, background history enrichment, and tmux frame visibility were not the dominant delay.
- Root `bun run dev` had a redundant intermediate Bun script process.
- The root overlay graph was eagerly included in the first shell import despite being unused for the initial Search surface.
- Controlled empty-shadow baseline before changes was roughly 2.1–2.2 seconds cold.
- The direct script change and the first lazy-boundary attempt produced much lower timings, but the first lazy attempt was attached to a dead renderer and was correctly rejected in review.

### Verification already run before the corrected lazy boundary

These passed before the latest loader correction:

- Focused startup suite: 19 pass, 0 fail.
- Full repository suite: 2592 pass, 10 skipped, 0 fail.
- Repository typecheck, lint, fmt, and build.

Those full gates must be rerun because `root-overlay-loader.tsx` and the canonical boundary changed afterward.

## Current startup implementation in the working tree

### Root development entrypoint

`package.json` changes:

```json
"dev": "bun apps/cli/src/main.ts"
```

This removes the nested `bun run --cwd apps/cli dev` process. Runtime smoke from the repository root succeeded, including path aliases and argument forwarding through Bun's script runner.

Regression test:

- `apps/cli/test/unit/architecture/dev-entrypoint.test.ts`

### Canonical overlay demand-loading

The initial wrong implementation in `ink-shell.tsx` has been removed.

Current intended ownership:

- `ink-shell.tsx`
  - no direct or duplicate `RootOverlayShell` renderer;
  - dead `rootSurface === "root-content" && rootOverlay` branch removed.
- `root-content-shell.tsx`
  - remains the canonical root-overlay renderer;
  - now renders `RootOverlayLoader` instead of statically importing `RootOverlayShell`.
- `root-overlay-loader.tsx`
  - dynamically imports `./root-overlay-shell`;
  - caches successful module evaluation;
  - resets the in-flight promise on failure so a later mount can retry;
  - displays a stable loading state;
  - displays a stable failure state;
  - owns Esc while the real overlay module is absent.

Current files:

- Modified: `apps/cli/src/app-shell/ink-shell.tsx`
- Modified: `apps/cli/src/app-shell/root-content-shell.tsx`
- New: `apps/cli/src/app-shell/root-overlay-loader.tsx`
- New: `apps/cli/test/unit/architecture/dev-entrypoint.test.ts`
- Modified: `package.json` (`scripts.dev` only belongs to this work; dependency hunks are unrelated user WIP)

The corrected architecture test and CLI typecheck passed once after moving the boundary.

## Immediate remaining startup work

Do these steps only; do not launch another multi-agent audit.

### 1. Clean up the profiling session

A tmux setup-screen probe may still exist because cleanup was interrupted when the safety classifier was unavailable:

```text
kunai-debug-lazy-1784270556233673286
```

If present, stop only that named session.

### 2. Prepare a safe completed-onboarding shadow profile

Do not run startup probes against live Kunai databases.

The prior shadow config was mutated to:

```json
{
  "onboardingVersion": 0,
  "downloadOnboardingDismissed": false
}
```

That caused the last timing probe to open Setup instead of Search. Create a new temporary shadow config/data/cache profile with:

```json
{
  "onboardingVersion": 2,
  "downloadOnboardingDismissed": true
}
```

Use empty shadow data/cache stores. Do not copy watch history unless explicitly approved again.

### 3. Verify the corrected demand-loaded path end-to-end

Run `bun run dev` in tmux using the completed-onboarding empty shadow profile.

Verify:

1. `Search title` is the first useful surface.
2. Typing immediately is retained.
3. Open `/notifications` immediately after Search appears.
4. The loading scaffold may appear briefly, then Notifications must render.
5. Press Esc during the loading scaffold and confirm the overlay closes.
6. Reopen Notifications and confirm the loader succeeds.
7. Open another overlay such as Help or History.
8. No blank terminal, duplicate footer, lost input, or crash.

Measure three cold runs with:

```text
BUN_RUNTIME_TRANSPILER_CACHE_PATH=0
```

Compare against the controlled 2.1–2.2 second baseline. Report all samples; do not claim a stable threshold from one run.

### 4. Add/adjust focused loader tests if needed

The current architecture test verifies:

- root `dev` directly launches `apps/cli/src/main.ts`;
- `ink-shell.tsx` does not reference `root-overlay-shell`;
- `root-content-shell.tsx` does not statically import `root-overlay-shell`;
- `root-content-shell.tsx` uses `root-overlay-loader`;
- `root-overlay-loader.tsx` contains the dynamic import.

If the Esc loading behavior is not already covered, add one focused render/input test using the local Ink harness. Prefer an injectable loader seam over source-string-only behavior tests if a small clean seam is possible.

### 5. Rerun verification

Run, in order:

```sh
bun run --cwd apps/cli test:file \
  test/unit/architecture/dev-entrypoint.test.ts \
  test/unit/architecture/boundary-imports.test.ts \
  test/unit/app/bootstrap/startup-setup.test.ts \
  test/unit/app/search/search-startup-policy.test.ts \
  test/unit/app-shell/browse-first-paint.useinput.test.tsx \
  test/unit/services/diagnostics/cli-startup-milestone.test.ts
bun run typecheck
bun run lint
bun run fmt
bun run test
bun run build
```

Protect the unrelated package WIP before and after `bun run fmt`.

### 6. Run one focused code review

Review only:

- root `package.json` `scripts.dev` hunk;
- `ink-shell.tsx` dead-branch removal;
- `root-content-shell.tsx` canonical loader boundary;
- `root-overlay-loader.tsx`;
- `dev-entrypoint.test.ts`.

Do not spawn a broad review fleet. The previous reviewer created hundreds of noisy task-list entries; those are not product work.

### 7. Finish startup work

Once the focused review is clean and all gates pass:

- mark startup Tasks 12 and 13 complete;
- do not commit unless the user explicitly asks;
- preserve all unrelated package/dependency changes.

## Notifications Inbox v2 implementation

After startup is stable, invoke `superpowers:subagent-driven-development` and execute:

`docs/superpowers/plans/2026-07-16-notifications-inbox-v2.md`

Use a fresh implementer and reviewer for each plan task. Do not use a worktree.

### Task 1 — Complete inbox datasets

- Add complete Active/Archive repository queries.
- Add NotificationService pass-throughs.
- Preserve existing limited queries.
- Test storage and service behavior.

### Task 2 — Action presentation and outcomes

- Centralize executable action labels/details/tones.
- Expose retry-download and update-app.
- Return handled/unsupported outcomes from `NotificationActionRouter`.
- Stop implicitly dismissing successful non-lifecycle actions.

### Task 3 — Sortable pure view model

- Add Attention/Newest/Type sorts.
- Sort before pagination.
- Add deterministic tie-breaking.
- Project selected row and companion rail.
- Handle malformed metadata and unknown kinds safely.
- Add nearest-selection helpers.

### Task 4 — Session-local navigation and stable targeting

- Add per-tab sort state.
- Preserve each tab's sort until overlay close.
- Target records by `dedupKey`, never raw repository index.
- Add `s` sort cycling and compact footer/help bindings.
- Remove the current raw-page targeting mismatch.

### Task 5 — Dense list and companion rail

- Extend `MediaListShell` with a custom rail slot if still needed.
- Render dense `ListRow` rows.
- Render selected-notice evidence and actions at wide widths.
- Collapse the rail at 100/72 columns.
- Use the local render-capture harness.

### Task 6 — Mark read after successful handled actions

- Add `notification-action-flow.ts`.
- Mark read only after confirmed handled non-lifecycle actions.
- Do not mark read after cancellation, unsupported actions, or failure.
- Remove implicit archive from play-now.
- Preserve selection across nested pickers and lifecycle changes.

### Notifications final verification

Run focused tests, then:

```sh
bun run typecheck
bun run lint
bun run fmt
bun run test
bun run build
```

Manual terminal smoke must cover:

- Active Attention/Newest/Type;
- Archive Newest/Type;
- per-tab sort retention;
- stable Enter target after sorting/paging;
- mark-read identity retention;
- successful action marks read but stays Active;
- play-now cancellation and confirmation;
- archive/delete nearest selection;
- 72/100/140-column layouts;
- active mpv playback remains healthy.

## Protected unrelated working-tree changes

These existed before this work and must not be reverted or absorbed accidentally:

- `apps/docs/package.json`
- `bun.lock`
- dependency/version/catalog changes in root `package.json`

The startup fix also changes root `package.json` at `scripts.dev`. If committing later, stage only the startup hunk without staging the unrelated dependency hunks.

## Current uncommitted/tracked-document expectations

Expected relevant working-tree items:

- startup source/test changes listed above;
- untracked `docs/superpowers/plans/2026-07-16-notifications-inbox-v2.md`;
- this handoff file;
- protected package/dependency WIP.

The Notifications design spec is already committed. The Notifications implementation plan and startup code are not committed unless a later session explicitly does so with user authorization.

## Task-list warning

The focused startup reviewer spawned hundreds of review-internal tasks numbered above the original session tasks. Ignore or delete those noisy review tasks. They are not planned product work.

The authoritative product task state is:

- Startup root-cause investigation: complete.
- Startup implementation: in progress.
- Startup end-to-end verification: pending.
- Notifications design/spec/plan: complete.
- Notifications implementation Tasks 1–6: pending.
- Durable Playlists manager: later independent slice.
- Loading/abortability audit: later handoff backlog item after Notifications.

## Definition of done for this handoff

This handoff is complete only when:

1. Startup work has a correct canonical lazy boundary or is intentionally reduced to the verified direct-script fix.
2. Startup cold measurements and immediate-overlay smokes are recorded from safe shadow data.
3. Focused tests, typecheck, lint, fmt, full tests, and build pass after the final startup code.
4. The six Notifications tasks are implemented and reviewed.
5. Notifications real-terminal smoke passes while playback is active.
6. Unrelated package/dependency WIP remains intact.
