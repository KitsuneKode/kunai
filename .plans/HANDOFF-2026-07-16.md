# Session Handoff — 2026-07-16 (catalog identity + shell polish)

Start the next session with: **"Read .plans/HANDOFF-2026-07-16.md and continue from it."**
Delete this file once its backlog is absorbed into the normal trackers.

## State as of this handoff

Branch `main`, clean tree, all gates green (`typecheck`, `lint`, `fmt`, `test`, `build`).
Session commits: `48912d32..bae3de92` (11 commits).

Landed this session (details in [catalog-identity-parity.md](./catalog-identity-parity.md) status
table and [plan-implementation-truth.md](./plan-implementation-truth.md) row "Catalog identity
parity"):

1. **Catalog identity parity core (Phases 0–3, 5)** — ARM crosswalk service + cache,
   `history_title_aliases` (migration 025), consolidator v2, budgeted background enrich
   backfill, fail-closed episode maps, dual-lane resolve adapter, cross-lane provider picker.
2. **`s` season hop** in all episode pickers (sentinel `EPISODE_PICKER_SWITCH_SEASON` in
   `session-picker.ts`), subtitle hints `s season · m watched`.
3. **Recent Decisions section** in `/diagnostics` (`diagnostics-panel-lines.ts`).

⚠️ Do not revert the widened `titleMatchesShellMode` (anime + tmdbId allowed in series mode)
without re-checking enrichment: ARM-proven anime would lose Videasy.

## Backlog for next session (priority order)

1. **Queue/notifications/playlists richer action UI** — the attention audit's open item
   ("richer action UI": inbox rows, sorting, detail actions). MUST start from the visual
   authority `kunai-sakura-canonical.html` / `.prototypes/` (manifest.json maps screen→source;
   `bun run prototype:serve`) — not from the .md specs.
2. **Loading/skeleton coherence + abortability** — audit loading states across surfaces for
   one consistent pattern; make long operations (resolve, enrich, downloads, search) visibly
   Esc-abortable everywhere.
3. **Manual smoke (needs a real terminal)** —
   - Death Note dual-lane: anime search → play ep via AllAnime → provider picker should list
     Videasy "via linked catalog id" → switch → history stays one AniList unit.
   - `s` season hop in search flow + mid-playback pickers.
   - `/diagnostics` shows Recent Decisions after a continue/provider decision.
4. **Catalog parity optional follow-ups** — dual-catalog search badges (plan Phase 4), Fribb
   offline pack (Phase 6).
5. **plans/ track residue (from the improve-track review)** —
   - Plan 010 harness (container-fake + Ink render capture for private AppRoot) — unblocks
     011/012/013/015.
   - Plan 008 residue: download alert still root-coupled (leaf subscription; feeds 013).
   - Plan 006 residue: measure eager provider registry cost before more lazy-load work.
   - Plan 014: burn down the 14 baselined legacy import edges.
   - Plan 016/017: delete stale HLS-dedup item; do the `.plans/` + docs trim.

## Where context lives

- Auto-memory: `project_catalog_identity_parity.md` (loads every session).
- Plan truth: [plan-implementation-truth.md](./plan-implementation-truth.md).
- This session's design decisions are in the commit messages of `a4b5cea9..bae3de92`.
