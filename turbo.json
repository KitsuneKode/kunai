{
  "$schema": "https://v2-10-0.turborepo.dev/schema.json",
  "tasks": {
    "transit": {
      "dependsOn": ["^transit"]
    },
    "typecheck": {
      "dependsOn": ["transit"],
      "outputs": []
    },
    "lint": {
      "cache": false,
      "outputs": []
    },
    "fmt": {
      "cache": false,
      "outputs": []
    },
    "fmt:check": {
      "cache": false,
      "dependsOn": ["typecheck"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["transit"],
      "cache": false,
      "outputs": []
    },
    "test:unit": {
      "dependsOn": ["transit"],
      "cache": false,
      "outputs": []
    },
    "test:integration": {
      "dependsOn": ["transit"],
      "cache": false,
      "outputs": []
    },
    "generate": {
      // apps/docs only. Writes generated-provenance.json, which is gitignored
      // (uncommitted) and refreshed every run, so it is not a declared output.
      // Caching this task replays its logs without recreating that file, leaving
      // @kunai/docs#test unable to import it in a clean checkout. Must always run.
      "cache": false,
      "outputs": []
    },
    "typecheck:app": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["^typecheck", "typecheck"],
      "outputs": [
        "dist/kunai.js",
        "dist/kunai.mjs",
        "dist/npm/**",
        "dist/assets/**",
        "dist/build-meta.json",
        ".next/**",
        "!.next/cache/**"
      ],
      "env": ["KUNAI_BUILD_ANALYZE"]
    },
    "build:binary:host": {
      "dependsOn": ["^typecheck", "typecheck"],
      "outputs": ["dist/bin/kunai-*", "dist/bin/SHA256SUMS", "dist/bin/SHA256SUMS.archives", "dist/bin/*.meta.json"],
      "env": ["KUNAI_BUILD_ANALYZE"]
    },
    "build:binaries": {
      "dependsOn": ["^typecheck", "typecheck"],
      "outputs": ["dist/bin/**"],
      "env": ["KUNAI_BUILD_ANALYZE"]
    },
    "check": {
      "dependsOn": ["typecheck", "lint", "fmt"],
      "outputs": []
    },
    "clean": {
      "cache": false
    },
    "@kunai/docs#test": {
      "dependsOn": ["transit", "generate"],
      "outputs": []
    }
  }
}
