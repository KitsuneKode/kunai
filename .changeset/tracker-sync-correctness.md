---
"@kitsunekode/kunai": patch
---

Make tracker sync use typed identities, a generation-safe SQLite outbox,
idempotent desired-state writes, fail-closed auth contracts, and ordered
cancellable shutdown. Removes the post-episode nudge that advertised a Connect
flow no palette could reach.
