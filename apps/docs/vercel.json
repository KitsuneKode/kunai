{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && bun install --frozen-lockfile",
  "buildCommand": "bun run generate && node ./node_modules/.bin/next build",
  "regions": ["iad1"]
}
