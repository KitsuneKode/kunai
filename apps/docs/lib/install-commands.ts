/**
 * Canonical install commands for home, interactive install, and releases.
 */
export const CANONICAL_INSTALL = "bun install -g @kitsunekode/kunai" as const;
export const CANONICAL_SETUP = "kunai --setup" as const;
